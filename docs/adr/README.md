# Architecture Decision Records

Single-page rationale per architectural decision. Each ADR records
context, decision, alternatives, and consequences. New ADRs are
sequentially numbered and never edited after acceptance — supersede
with a new ADR if the call changes.

## Foundations epic

- [ADR-001](./ADR-001-drizzle-as-postgres-orm.md) — Drizzle as Postgres ORM
- [ADR-002](./ADR-002-inngest-as-job-orchestration.md) — Inngest as job orchestration
- [ADR-003](./ADR-003-neo4j-as-vector-store.md) — Neo4j as vector store
- [ADR-004](./ADR-004-env-vars-not-secret-manager.md) — Environment variables, not Google Secret Manager
- [ADR-005](./ADR-005-single-version-monorepo.md) — Single-version monorepo via Changesets
- [ADR-006](./ADR-006-better-auth-bound-to-canonical-users.md) — Better Auth bound to canonical `auth.users`

## Agent Runtime epic

- [ADR-007](./ADR-007-docker-as-code-sandbox.md) — Docker as vendor-neutral code sandbox
- [ADR-008](./ADR-008-skills-filesystem-first.md) — Skills as filesystem-first with DB augmentation
- [ADR-009](./ADR-009-unified-capability-tool-model.md) — Unified capability/tool model via `surfaces`
- [ADR-010](./ADR-010-subagent-fanout-via-inngest.md) — Subagent fanout via Inngest invoke
- [ADR-011](./ADR-011-vercel-sandbox-driver.md) — Vercel Sandbox driver for Vercel Functions

## Marketplace epic

- [ADR-012](./ADR-012-connector-dual-write-pattern.md) — Connector dual-write to Postgres + Neo4j
- [ADR-013](./ADR-013-oxagen-plugins-capability-packs.md) — Oxagen Plugins: first-party capability packs as a fourth plugin type
- [ADR-014](./ADR-014-workspace-scoped-mcp-registry-single-default.md) — Workspace-scoped MCP registries with a single-default state machine

## Developer workflow epic

- [ADR-015](./ADR-015-graph-edge-driven-git-hooks-and-biome.md) — Graph-edge-driven git hooks (Vitest import-graph) + Biome formatting

## CLI & Local Agent Runtime epic

- [ADR-016](./ADR-016-oxagen-cli-daemon-live-code-graph.md) — Oxagen CLI daemon: live code-graph memory fed by coding-agent hooks (Proposed)

## Not yet filed under an epic

The lists above stopped being extended at ADR-016 while the directory
kept growing, so an index that read as complete was missing most of it.
These are every ADR the sections above do not already link, oldest
first. Move a row into its epic when one fits.

- [ADR-017](./ADR-017-opentelemetry-distributed-tracing.md) — ADR-017 — OpenTelemetry Distributed Tracing
- [ADR-018](./ADR-018-cli-workspace-graph-bidirectional-sync.md) — CLI ↔ Workspace Graph Bidirectional Sync
- [ADR-019](./ADR-019-unified-agent-engine.md) — Unified agent engine — one brain across CLI and platform
- [ADR-020](./ADR-020-per-workspace-github-write-credentials.md) — Per-workspace GitHub write credentials
- [ADR-021](./ADR-021-inference-doctrine.md) — Inference doctrine — deterministic-first, cache-aligned, structured-tool agentic coding
- [ADR-022](./ADR-022-capability-naming-standard.md) — Capability & tool naming standard
- [ADR-023](./ADR-023-cli-fleet-session-event-log.md) — sessions as append-only event logs, views as renderers
- [ADR-024](./ADR-024-namespaced-agent-identity.md) — ADR-024 — Namespaced, immutable agent identity (`org_ns.workspace_ns.agent_slug`)
- [ADR-025](./ADR-025-verb-first-snake-naming.md) — Verb-first snake_case capability naming
- [ADR-026](./ADR-026-mobile-feature-parity.md) — ADR-026 — Mobile feature parity is law, enforced by a manifest gate
- [ADR-027](./ADR-027-multi-tenant-github-app-connect.md) — Multi-tenant GitHub App connect (identity leg + installation registry)
- [ADR-028](./ADR-028-time-travel-replay.md) — deterministic session records, bisect, resume, and failure→eval distillation
- [ADR-029](./ADR-029-mutation-verifier-gate.md) — The mutation verifier gate — every green turn must prove its tests witness the fix
- [ADR-030](./ADR-030-speculative-tool-execution.md) — Speculative tool execution — prefetch the model's next reads while it thinks
- [ADR-031](./ADR-031-platform-storage-ontology.md) — Platform Storage Ontology — a drift-aware, machine-readable self-model of the platform's storage layer
- [ADR-032](./ADR-032-unified-chat-session-state.md) — ADR-032 — Unified chat session state (chat_ux_v2)
- [ADR-033](./ADR-033-stella-engine-core.md) — Adopt the Stella Rust engine as the platform agent core
- [ADR-034](./ADR-034-customer-capability-packages.md) — Customer-built capability packages (`.cap`)
- [ADR-035](./ADR-035-consume-context-graph-protocol-directly.md) — Consume the Context Graph Protocol directly via pinned conformance fixtures
- [ADR-036](./ADR-036-adopt-cgp-typescript-sdk.md) — Adopt the official CGP TypeScript SDK as the canonical type source
- [ADR-037](./ADR-037-test-doubles-must-fail-when-their-original-moves.md) — A test double must fail when the thing it doubles moves
- [ADR-038](./ADR-038-adopt-standing-decisions-scr-corpus.md) — Adopt org standing decisions as a Steering Context Record corpus
- [ADR-039](./ADR-039-centralize-scr-enforcement-in-oxagen.md) — Centralize SCR enforcement in oxagen rather than replicating it
- [ADR-040](./ADR-040-governance-plane-refocus.md) — Refocus Oxagen as an engine-agnostic governance plane
- [ADR-041](./ADR-041-canonical-json-one-rule-not-one-implementation.md) — Canonical JSON — one rule, not one implementation
- [ADR-042](./ADR-042-tenant-data-planes.md) — Organisation-scoped data planes: every store switches at the tenant
- [ADR-043](./ADR-043-runtime-excision.md) — Excise the agent runtime: Oxagen governs agents, it does not run them
- [ADR-044](./ADR-044-memory-record-merge-is-total.md) — Every field of a memory record has a merge rule
- [ADR-045](./ADR-045-pin-cross-repo-reusable-workflows.md) — Cross-repo reusable workflows are pinned to a commit
- [ADR-046](./ADR-046-per-commit-ci-concurrency-on-main.md) — A push to main gets its own CI concurrency group
- [ADR-047](./ADR-047-no-provider-posture-matrix.md) — No provider-posture matrix; handle divergence at the gateway
- [ADR-048](./ADR-048-one-path-glob.md) — One path glob, in a package with no dependencies
- [ADR-049](./ADR-049-dod-recheck-reruns-the-old-run.md) — The DoD recheck re-runs the old run rather than reporting a new check
- [ADR-050](./ADR-050-secret-access-in-the-main-audit-log.md) — Privileged secret access is recorded in the main audit log, not only beside it
- [ADR-051](./ADR-051-context-records-enter-the-turn-as-volatile-policy.md) — A workspace's context records enter the turn as volatile policy, not as prefix
- [ADR-052](./ADR-052-governed-action-as-the-billable-unit.md) — The governed action is the billable unit; tokens are reported, never billed
- [ADR-053](./ADR-053-in-app-agent-on-stella-serve-and-funding-sources.md) — The in-app agent runs on Stella's headless engine, and a token is billed only when Oxagen paid for it
- [ADR-054](./ADR-054-the-migration-connection-carries-the-rls-bypass.md) — The migration connection carries `app.rls_bypass`; the registries policy keeps its narrower WITH CHECK
- [ADR-055](./ADR-055-gau-buckets-and-contracted-rates.md) — Subscriptions carry monthly buckets of governed action units, bought in unit quantities at a per-customer contracted rate; two billing modes; one settlement ledger with `paid` as its only terminal state
- [ADR-056](./ADR-056-run-control-connection-points-and-the-model-proxy-decision.md) — Run controls are delivered through the hook adapter with a per-recipient delivery mode; the model proxy is its own decision; `tacho.control_commands` is widened rather than renamed; operator steer and mass steer ship, agent-originated messages wait
- [ADR-057](./ADR-057-agents-the-definition-of-record-budgets-in-micros-enrollment.md) — Agents: the definition of record is `.oxagen/agents/<slug>.toml` in git with the version row as its cache; budgets carry `Money`; rotate, suspend and retire are the identity writes; an API key's scope purpose decides who the bearer is
- [ADR-058](./ADR-058-run-record-placement-retention-default-and-digest-only.md) — The Postgres ledger stays the run record write path with bodies in object storage; seven-year retention from the seal with `digest_only` as a pinned policy recorded as a gap; the replay grade is computed once at seal and `fork` mints an attempt behind it
- [ADR-059](./ADR-059-mandates-consequence-roles-decision-seam-and-ledger.md) — Mandates: consequence tags name the org roles accountable for them with a workspace override; the kernel's decision gate checks an agent's mandate and reserves against an append-only ledger under a row lock; two-person mandates are cut
- [ADR-060](./ADR-060-spend-price-book-as-data-rollup-store-budgets-in-micros.md) — Spend: the price book is data seeded from the in-code card, the cost rollup is a Postgres index rebuilt from frames after each seal and nightly with a basis on every figure, budgets and every spend figure carry micros, and the spend-budget gate reads a counter the recorders keep
- [ADR-061](./ADR-061-steering-governance-mode-thresholds-and-the-reflector.md) — Steering: governance mode from `governance.toml` (`team` by default), no promotion thresholds, the reflector deferred; proposals in their own table, Stella's record hash, the GitHub connection as the workspace's repository, checks in the request, the ledger length as the steering version
- [ADR-062](./ADR-062-findings-providers-fix-application-counterfactual-rule.md) — Findings: no provider importer ships with reconciliation cut; a fix Oxagen does not hold is recorded against the finding with the request id its audit row carries, and later passes cite only runs after it; a saving is measured minus counterfactual over the cited runs at each run's input price, with confidence from the share of cited calls the counterfactual covers
- [ADR-063](./ADR-063-organization-the-permission-catalogue.md) — A permission is a named bundle of registered capabilities in seven groups; roles are edited only where the kernel reads them; a granter cannot hand out more than they hold; custom roles are agent roles; a role name is unique per org and scope kind
- [ADR-064](./ADR-064-proof-the-witness-runner-plane.md) — Proof: the witness runner is the one Oxagen-operated execution plane, verdicts are ingested from `proof.observed` frames before authoring lands, the run's verdict is aggregated once from its verdict rows, and the disclosure grain is `L0` unless a signed-in Owner or Admin raises it
- [ADR-065](./ADR-065-onboarding-gate-shape-installer-path-provisional-workspace.md) — Onboarding: the gate is state on the organization (`org.onboarding_state`) and a rail over real pages, opened by `create_org` and closed only by the first frame; the installer is the CLI path with a single-use enrollment token (`enroll_host`); the provisional workspace ships and the conversion offer waits
- [ADR-066](./ADR-066-two-names-agent-control-plane-and-agent-fleet-management.md) — Oxagen has two names, the agent control plane and agent fleet management; spend is part of the fleet; the agent asks for the keys (the two names superseded by ADR-067)
- [ADR-067](./ADR-067-mission-control-leads-the-control-plane-is-the-category.md) — Mission Control is the experience and the agent control plane the category; completion is optional; every claim states its scope; the message registry is the source of copy
- [ADR-068](./ADR-068-the-org-only-workspace-sentinel-is-shared.md) — One org-only workspace sentinel, exported from `@oxagen/oxagen` and used by both surface builders; `TenantScopeError` is a 400 on the API
- [ADR-069](./ADR-069-roles-are-editable-on-every-tier.md) — Roles are editable on every tier; the delegation ceiling and the role gate are what protect the write, and the Roles page prints whether the kernel resolves the grants (supersedes ADR-063 decision 3)
- [ADR-070](./ADR-070-auto-approval-rules-the-policy-matrix-the-rule-store-standing-approvals.md) — Auto-approval rules: the policy matrix is four floors in code, the rules are the second clause of the rule set already stored in workspace settings, a standing approval is rule-named and bound to the workspace and the call digest, and the evaluation is recorded on the approval rather than recomputed
- [ADR-071](./ADR-071-a-kill-switch-outlives-its-target-deletes-are-refused.md) — A kill switch outlives its target: deleting the credential or MCP server a switch names is refused as a conflict rather than silently dismantling the control
- [ADR-072](./ADR-072-tools-classification-vocabulary-assurance-hosting-observed-schema-source.md) — Tools: one classification object with the spec's tag starter set and customer tags; assurance and observed schemas stay cut; a kill switch is matched by one resource-scope digest on both sides; the gateway re-checks on a deny-generation bump
- [ADR-073](./ADR-073-an-api-key-names-a-workspace.md) — An API key names a workspace, and so does the page that mints it
- [ADR-074](./ADR-074-a-read-under-the-org-only-sentinel-is-checked-against-the-policy-manifest.md) — A read under the org-only workspace sentinel is checked against the policy manifest: the sentinel lives in `@oxagen/tenancy`, an org-level surface reaches a non-`org_only` table through `withSystemDb` with an explicit org fence or by re-entering a real workspace, and `check:org-sentinel-reads` fails CI on the rest
- [ADR-075](./ADR-075-one-writer-for-the-account-preference-row.md) — One capability writes `auth.user_preferences` and it carries every field the read returns; `update_user_preferences` stays deleted with no shim and `apps/app_deprecated` ports to `set_preferences`; a preference the product reads and no surface can set is a dead value
- [ADR-076](./ADR-076-a-general-run-spec-states-what-the-run-did.md) — A `general` run's spec records what that run actually ran under: `sandbox_required` is a boolean on the general kind and stays `true` on a repo edit, the tool allowlist is bounded above the capability catalogue, and a spec section that cannot state the truth about a run is a schema defect
- [ADR-077](./ADR-077-attribution-columns-are-by-id.md) — An attribution column is named `<verb>_by_id` (`created_by_id`, `updated_by_id`, `deleted_by_id`, and per-table columns like `invited_by_id`) whenever it references a user or principal, never the bare `<verb>_by` form; a value column that isn't a reference (`control.approvals.resolved_by`) keeps the bare text form; one idempotent migration renames every match across every schema from `information_schema`, and a unit test guards the spelling going forward
- [ADR-078](./ADR-078-wrapped-and-connected-are-two-enforcement-tiers-neither-dominates.md) — wrapped (`harness`) and connected (`gateway`) are two enforcement tiers and neither dominates: wrapped is broader and client-attested, connected is narrower and server-enforced; the local gateway is a proxy, not a second materialiser
- [ADR-079](./ADR-079-an-oxagen-login-key-acts-for-its-creator-on-host-enrollment.md) — An `oxagen login` key acts for its creator on Tacho host enrollment; machine-bound keys never do
- [ADR-080](./ADR-080-stella-is-wrapped-through-its-hooks-until-it-speaks-the-tacho-contract-natively.md) — Stella is wrapped through its hooks until it speaks the Tacho contract natively
- [ADR-081](./ADR-081-app-layers-retired-at-mission-control-cutover.md) — The `app` layer is retired at the Mission Control cutover for every capability rev1 does not surface
- [ADR-082](./ADR-082-a-rate-limiter-that-cannot-reach-its-counters-degrades-rather-than-denies.md) — A rate limiter that cannot reach its counters degrades to the per-instance ceiling rather than denying every request
- [ADR-083](./ADR-083-one-client-ip-derivation-and-the-edge-rewrites-the-header.md) — One client-IP derivation, and the edge rewrites the `X-Forwarded-For` it is not allowed to trust
- [ADR-084](./ADR-084-the-session-person-is-a-principal-oxagen-issues.md) — the person behind a Tacho session is a principal Oxagen issues, and nothing derived from the harness-reported address is stored: a stable per-person value computed from producer-supplied input and readable by the principal who supplied it is a dictionary oracle however it is hashed or keyed, and the row already carried `initiating_principal_id`
- [ADR-085](./ADR-085-a-refunded-gau-purchase-withdraws-what-is-left.md) — A refunded or disputed GAU purchase withdraws what is left of its units from the organisation's current bucket, clamped at zero, and records what it could not recover

- [ADR-086](./ADR-086-an-org-only-read-of-a-workspace-scoped-table-raises.md) — An org-only scope sets a workspace GUC that is not a uuid, so a read of a workspace-scoped table RAISES at plan time instead of silently under-reading; `withOrgDb` is the plane-aware organisation-wide read seam with the org fence still in the database; `check:org-sentinel-reads` retires (supersedes ADR-074 decision 3)
- [ADR-087](./ADR-087-the-neo4j-tenancy-seam-constructs-scoping-rather-than-validating-it.md) — **Proposed.** The Neo4j tenancy seam constructs scoping rather than validating it. Added to this index by #3233's follow-up: the ADR landed on `main` without an index entry, and an ADR absent from the index is one nobody finds.
- [ADR-088](./ADR-088-a-credential-follows-the-service-that-reads-it.md) — A credential is registered for every service that reads it: `apps/app` reaches every capability in-process through the kernel seam, so the GitHub App's client id, slug, state secret, client secret, app id and private key — and the ingestion-crypto material that opens a stored OAuth token — name `app` as well as `api`. Routing those capabilities through the API instead is rejected: there is no app→api HTTP path in this repo, the hop would need a user-impersonating credential and a second error taxonomy, and the boundary it buys is illusory where both services share one instance, one IAM role and `BETTER_AUTH_SECRET`. Enforced by iterating the constants the handlers read, because `env:check` checks the opposite direction and reports this defect clean
- [ADR-089](./ADR-089-an-on-demand-read-lives-in-a-use-server-module.md) — `src/features/**` may import `kernelRead`, under the same condition that already governs `kernelWrite`: only from a `"use server"` module that resolves its own viewer. A read whose record is a live third-party call, on a surface the shell renders above every page, has no route render to belong to; a `DataSource` port method for it would have no caller (INV-17) and a layout read would charge every page for the round trip. A read from a module with no directive stays refused.
- [ADR-090](./ADR-090-oxagen-governs-skill-resolution.md) — Oxagen governs skill **resolution** and still never runs a skill, reversing the 2026-09-15 narrowing that had shipped Skills as an observed inventory of the names a harness happened to report. That narrowing read spec §2's "no skills engine" as "no skills surface"; running a skill and resolving one are separate acts, and ADR-043 cut only the first. `.oxagen/skills.toml` is the config of record, read through the repository binding and changed only by pull request; a workspace is created with skills off; withholding happens before ranking and the agent is told the count and reason class, never the name; a run pins its config version so replay resolves that version; an unbound repository stops the loop before the first model call and there is no `allow`; reflection is research-only, quarantined, and outside the sealed chain
- [ADR-091](./ADR-091-one-record-steers-one-agent.md) — One record steers one agent: active `must` and `should` context records compile into the signed policy bundle's `context.system`, which the collector hands the agent at session start. The bundle etag already covers the text and the steering version is already the ledger length, so a merge reaches every enrolled host on its next poll with no new counter. `may`/`info` stay out, whole records are dropped past the host's 16,384-character limit, and new governance ceremony is frozen until a merged record shows up in a real run. Supersedes ADR-051's delivery; reopens #2592 against this seam
- [ADR-092](./ADR-092-an-abandoned-assistant-turn-is-owned-not-cancelled.md): A turn the person walks away from is owned to completion, not cancelled. The flyout keeps one thread per workspace and writes everything a turn produces (reply, run, conversation id, refusal, parked writes, and the end of `pending`) to the thread it was asked in, whatever is on screen when it resolves. Cancel-on-navigate is rejected for three reasons. It discards an answer the person asked for and paid for. It can stop a turn partway through a governed write. It would be a second cancel mechanism beside #2953's cancel on every run. The discarding generation guard it replaces had to be written during render under an `eslint-disable` to win a race. Routing by the workspace a turn was asked in has no race. Stopping a turn on purpose stays with #2953.
- [ADR-093](./ADR-093-one-assembler-decides-what-reaches-the-agent-and-records-what-it-cut.md) — One assembler, `assembleSteering(run, budget)`, returns a stable prefix, a volatile selection and a manifest recorded as a `steering.manifest` frame. One item type, `SteeringItem`. Oxagen's injection points are exactly five because the harness owns the context window. The index sits behind a port, Postgres first and the graph in Phase 3. Skills are governed files delivered by sync. `packages/context-provider` is the assembler's home and `packages/engram` is deleted or folded in (amends ADR-008, ADR-051, ADR-090; builds on ADR-091)
- [ADR-094](./ADR-094-tachod-grows-into-the-gateway-a-loopback-model-proxy-and-an-mcp-aggregator.md) — `tachod` grows into the gateway: hook adapter, loopback model proxy, MCP aggregator, control channel. Loopback because it adds no hop, keeps prompt bodies and the vendor credential on the machine, and makes metering observed for every harness. Subscription logins through a base URL proxy were validated by the maintainer on 2026-09-18. Phase 4, in build now (closes the decision ADR-056 left open)
- [ADR-095](./ADR-095-the-tier-ladder-is-four-words-computed-from-what-was-routed.md) — The tier ladder is four words, computed from what was actually routed: observe, harness, gateway, contained. Each word has claims it may and may not make; only `contained` earns "enforced" against the machine's operator, and a control claim always carries its scope (amends ADR-078 §1; keeps its §2)
- [ADR-096](./ADR-096-oxagen-may-contain-the-process-that-runs-turns-the-contained-tier.md) — Sandbox is the top tier, not the only tier, and hooks stay. `oxagen run -- <agent>` launches an agent under an OS sandbox with egress limited to the gateway, the hook configuration pinned read-only, and a filesystem policy, for CI, headless runs, cloud runners and managed devices first, never mandatory on a developer's laptop. The witness runner is built on the same launcher (amends ADR-043 by one sentence, and ADR-064)
- [ADR-097](./ADR-097-steering-and-gating-are-two-planes-authored-on-one-surface-and-compiled-twice.md) — Steering and gating are two planes that never merge: steering is what the model reads (advisory, ranked, budgeted, may be dropped) and gating is what the kernel refuses (deterministic, never budgeted, works when Neo4j is down). One authoring surface, two compilations, and every gate emits a one-line gate notice into steering. Storage stays plural with one writer per fact; nothing is collapsed into Neo4j. Precedence is fixed in one place. Steering is the hub with seven tabs, and Skills and Ontology lose their own nav entries (amends ADR-090, ADR-008). ADR-091, the Phase 0 decision, is indexed by its own PR (#3289). Numbered 097 although it reads first of the five: ADR-092 was taken by #3292 while this set was being written
- [ADR-098](./ADR-098-organisation-graph-provisioning-pooled-by-default.md) — An organisation's graph is placed by one `OrgGraphProvisioner` (pooled, cypher, aura). Free and trial organisations are pooled by rule, and every organisation is pooled until a deployment sets `NEO4J_ORG_PROVISIONER=cypher`. A paid organisation on `cypher` gets `org-<namespace>` at creation, routed by `org.data_planes.graph_database`. Aura is refused until an instance exists. Community Edition in dev and CI means the per-org path is proven by unit tests and the pooled path by a real-Neo4j cross-tenant probe. The tenancy seam now requires every row-selecting MATCH pattern to carry its own anchor.
- [ADR-099](./ADR-099-a-workspace-is-born-with-its-main-repository.md) — A workspace is born with its main repository, and a repository is main for at most one workspace: `create_workspace` requires `mainRepo` and writes the workspace, its GitHub connection, the version-1 binding and its `role = 'main'` head in one transaction, so a workspace without a main repo is never written. The installation is the one the org's stored GitHub authorization reaches on the repository's owner account, never a caller-supplied id, because a chosen id would let one tenant mint tokens for another account's installation. The global unique index `repository_binding_heads_main_repository_uq` is the race backstop; `link_repository` refuses another workspace's main (`main_repo_claimed`) because §10.1 opens Context PRs on the linked repository; `unlink_repository` deletes only the head and keeps binding versions as evidence; the org's first workspace from `create_org` is the spec's one exception, provisional for 14 days. The §11.4 follow-through (events, issues, code graph) is deferred to the v2 descriptors
- [ADR-100](./ADR-100-frame-bodies-are-captured-by-default-redacted-at-the-host-capped-and-governed-by-retention.md) — Every recorder captures the content its frames are about, with no flag to turn on: the hooks, the model proxy, the MCP gateway and the in-app assistant. Redaction runs where the bytes are produced and the body digest names the redacted bytes, never the bytes that arrived. One MiB per body and four per batch; past the cap there is no body and the frame keeps its digest, because truncated bytes digest to a value naming nothing that existed. `retention.mode: digest_only` keeps the chain and ships none of it. The event registry now names which events complete a step, so the seal, the transcript, the Runs page and the cost rollup stop each holding their own list of two names the only producer in the tree does not write.
- [ADR-101](./ADR-101-four-first-class-harnesses.md) — Claude Code, Codex, Cursor and Stella are the four wrapped harnesses, and none of them is the default. Tacho wraps Cursor through `~/.cursor/hooks.json` and an adapter that translates Cursor's hook shape into Claude Code's, so one policy rule covers every harness; Cursor's model calls stay on Cursor's own backend, so Tacho records its actions and does not meter its spend. Everything Oxagen exports for an agent — a skill, an agent definition, a rule, an MCP entry, a hook — must load in all four, or its PR names the harness that cannot and why. The ADR carries the table of where each harness reads each artifact.
- [ADR-102](./ADR-102-a-limit-change-merges-under-the-mandates-row-lock.md) — `update_mandate_limits` takes `limitChanges` beside `limits`: changes keyed by measure, merged over the stored record inside the transaction that already locks the row, at two depths. A caller that read the mandate, merged its edit and posted the whole record back held a snapshot nobody locked, so two operators changing different bounds on one mandate could each write a complete record and the later write would restore the bound the earlier one lowered. Replacement stays exactly as it was, because it is the only way to delete a bound and it is the right primitive for a caller that holds the record. The two are mutually exclusive; the contract refuses both.
- [ADR-103](./ADR-103-the-price-book-records-its-own-initialization.md) — The price book records its own initialization in `cost.price_book_initializations`: the instant, and the catalogs that answered completely at it. The cold-start floor, which backdates a key the book has never priced to an instant before every frame, must fire only for a source that was down at initialization, and that set cannot be read back off `price_entries`, because precedence drops every row of a catalog whose models all lost to a higher-precedence source. Reading it off the rows floored such a catalog's first unique model and let the next rollup change costs on runs that had already settled. Per install, no RLS, written in the same transaction as the book's first rows. A missing record means the sources are unknown, and unknown floors nothing, because an unpriced frame can be repaired and a repriced settled run cannot.
- [ADR-104](./ADR-104-a-harness-label-round-trips-whatever-it-holds.md) — `list_skills`' harness aggregation moves from a newline-delimited `string_agg` to `jsonb_agg`, so a harness label round-trips byte-for-byte no matter what it contains, instead of splitting a label with a newline into invented harnesses or failing the whole read over one session's empty label. The wire schema (`agent.harness`) is unchanged — tightening an already-deployed ingestion contract to fix a downstream read would drop data from harnesses already running in the field. `SKILL_HARNESS_CAP` (20) bounds each row's returned harness list, with `harnessCount` carrying the true distinct count past the cap (#3103).
- [ADR-105](./ADR-105-an-archived-workspace-accepts-no-machine-credential.md) — An API key bound to an archived workspace no longer authenticates, and archiving revokes nothing. `resolveApiKey` reads the workspace it resolved and refuses with `workspace_archived`, so the rule holds on every surface that resolves a key and applies to keys stranded by archivals that happened before it, with no migration. Refusing to archive while a live key exists would have left those stranded keys working and turned archival into a credential hunt; revoking them would have made a one-way door out of a filing action. `archive_workspace` reports the count as `suspendedApiKeys`.
- [ADR-106](./ADR-106-retirement-revokes-what-it-scoped.md) — `retire_agent` now also revokes every mandate still `active` or `draft` against the agent's principal, in the same transaction that archives the agent, suspends the principal, and revokes credentials and hosts, instead of leaving a mandate that reads `active` bound to a principal that can never draw on it. Retirement does not refuse while a mandate is live: an operator retiring a compromised or misbehaving agent needs its authority cut in the same action, not blocked behind first tracking down and revoking every mandate by hand. `resolveAgent`, shared by `request_mandate` and `grant_mandate`, now takes a `{ lock: true }` option that locks the agent row `FOR SHARE` for the duration of the mandate write, so a grant racing a retirement resolves in a fixed order rather than either winning silently (#3124).
- [ADR-107](./ADR-107-list-mandates-admits-the-roles-request-mandate-admits.md): `list_mandates` and `get_mandate` now admit exactly the roles `request_mandate`'s handler already enforces (org Owner, Admin, Billing, Compliance unnarrowed; workspace Owner and Member narrowed to their own agents' mandates, or a mandate they requested themselves for any agent), and no contract in this family grants a nonexistent org "Member" role, since that role does not exist anywhere in the seeded IAM system. `readerFilter` and `blindSpotOf`'s `reader_scope` branch already implemented the creator half of this narrowing; widening the grants is what makes it reachable on both the list and the detail page, which previously refused every workspace Owner and Member outright. The requester half closes a second gap `request_mandate` admitting any agent, not only ones the caller created, otherwise left open: a requester who did not operate the agent could create a draft they could never read back (#3138, #3440).
- [ADR-108](./ADR-108-a-mandate-limit-carries-its-own-measure-kind.md): A mandate limit's `kind` (money or count) is worked out once, at write time, by `assertToolsDeclareMeasures` from the tool's declared measure type, and stored on the limit and on the read-side authority, instead of every downstream reader guessing money-or-count from whether `currencyOrUnit` looks like an ISO 4217 code. The guess was wrong whenever a tool legitimately declared a count denominated in a currency code, printing a whole-unit count of 50 as $0.00 while the gate correctly enforced 50 counted units. A limit written before this change has no stored `kind` and takes a documented, single-location fallback (`legacyMeasureKindGuess`) rather than a backfill migration; two matched tools disagreeing on a measure's kind now refuse the write (`measure_kind_conflict`) rather than silently picking one (#3130).
- [ADR-109](./ADR-109-the-book-has-one-manuscript.md) — The book "Engineering Deterministic AI Coding Agents" has one manuscript under `packages/database/seed-assets/books/manuscript/`. Its two editions (the field manual and the page-flip reader) and the migration that carries them to production are generated by `tools/scripts/build-book-editions.mjs`. Three hand-kept copies of the text had drifted: one per edition, and a third embedded in a migration, because production takes book HTML only from `atlas migrate apply`. A test rebuilds in memory and fails when an output is stale, when the prose breaks a rule a script can check, or when a part lacks its checklist and metrics.
